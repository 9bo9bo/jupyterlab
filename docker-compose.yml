version: "3.7"
services:
  jupyter:
    image: jupyter/datascience-notebook
    ports:
      - "8888:8888"
    volumes:
      - "./work:/home/jovyan/work:cached"
    environment:
      TZ: "Asia/Tokyo"
      JUPYTER_ENABLE_LAB: "yes"
